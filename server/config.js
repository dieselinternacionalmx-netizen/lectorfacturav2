// ============================================
// CONFIGURACIÓN DEL SISTEMA
// ============================================
// Edita estos valores según tus necesidades

module.exports = {
    // Nombre de la carpeta donde se colocarán los PDFs a procesar
    // Puedes cambiar este nombre a lo que prefieras
    PDF_FOLDER_NAME: 'FACTURAS_PDF',

    // Puerto del servidor (normalmente no necesitas cambiar esto)
    SERVER_PORT: 3000,

    // Nombre de la base de datos
    DATABASE_NAME: 'invoices.db'
};
